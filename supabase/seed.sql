-- Seed data for local development only
-- Run: supabase db reset

-- Note: Auth users must be created via Supabase Auth, not inserted directly.
-- Use the dashboard or `supabase auth create-user` for test accounts.

-- Example org (insert after creating a test user via auth)
-- insert into public.organizations (name, slug, owner_id)
-- values ('Acme Agency', 'acme-agency', '<your-user-id>');
